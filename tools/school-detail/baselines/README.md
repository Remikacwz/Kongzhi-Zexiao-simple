# 生成基线

## zxb_canonical.json

`generate_pages.py` 的**唯一可信基线**，用于复现 `school_detail/` 下的 133 个页面。

验证记录（2026-09-14）：用该基线重新生成 133 个页面，与线上 `school_detail/` **逐字节 100% 一致**。

```
ZXB_PARSED=tools/school-detail/baselines/zxb_canonical.json \
ZXB_OUT_DIR=<临时目录> \
ZEXIAO_JS=<临时文件> \
python tools/school-detail/generate_pages.py
```

基线已包含的历史修正：
- 录取分析补全（北京邮电大学 / 中国民航大学 / 北京科技大学 等）
- 招生计划：误抓科目代码的 6 格 → 暂无数据（江苏科技大学 999×4、广西科技大学 903×2）
- 招生计划：湖南科技大学按原文修正 → 18 / 19 / 暂无数据
- 数据富化：电子科技大学、西安理工大学复试分数线；青岛科技大学各科均分

⚠️ 重生成请只写到临时目录，比对通过后再覆盖 `school_detail/`。

## 解析器已知限制（2026-09-14 验证）

用原始 `parse_zxb.py` + `major_parse.py` 重新解析 `.qrcheck/zxb_text.txt`，与标准基线逐专业比对，
招生计划只有 9 个专业不同 —— 即下面 9 项修正**必须依赖基线数据**，重新解析无法自动得到：

| 学校 | 专业 | 原文情况 | 解析结果 | 基线值 |
|---|---|---|---|---|
| 江苏科技大学 | 控制科学与工程 / 控制工程 / 低空技术与工程 / 仪器仪表工程 | 表中无招生计划列 | 误抓复试科目代码 999 | 暂无数据 |
| 广西科技大学 | 控制工程 / 机器人工程 | 表中无招生计划列 | 误抓复试科目代码 903 | 暂无数据 |
| 湖南科技大学 | 081100 控制科学与工程 | 「拟招生人数」列 → 全日制：18 | 误抓研究方向序号 01 → 1 | 18 |
| 湖南科技大学 | 085406 控制工程 | 「拟招生人数」列 → 全日制：19 | 漏抓 → 空 | 19 |
| 湖南科技大学 | 085410 人工智能 | 「第一年招生」，无计划数 | 误抓下一个专业代码 0802J1 → 802 | 暂无数据 |

### 若要修解析器，需要解决两件事

1. **「全日制：N」定位**：该行不在 `major_parse.py` 中 `tail` / `following` 构造的块范围内，规则加在
   `_plan_from` 调用处不会命中，需要先调整块的切分逻辑。
2. **误抓判定要收紧**：曾尝试在 `_plan_from` 后用「该数字在原文中紧跟中文」判为科目代码，
   结果把杭州电子科技大学 081100（100）、浙江工业大学 085406（255）、重庆邮电大学 085406（170）
   这三个**合法计划数**误判为空 —— 原因是一段文字里的分数/人数也会形成「数字+中文」。

在两件事都解决并验证「与标准基线 0 差异」之前，**不要用重新解析的结果覆盖 `zxb_canonical.json`**。
